<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    tools:context="com.vpay365.payments.TransferFragment">

    <TextView
        android:id="@+id/label_vpay_wallet_address"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/vpay_wallet_address"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/copyAddressButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textAllCaps="false"
        android:textSize="14sp"
        android:background="@drawable/vpay_state_button"
        app:layout_constraintTop_toBottomOf="@id/label_vpay_wallet_address"/>

    <TextView
        android:id="@+id/labelCopyAddress"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/tap_to_copy"
        app:layout_constraintTop_toBottomOf="@id/copyAddressButton" />

</android.support.constraint.ConstraintLayout>